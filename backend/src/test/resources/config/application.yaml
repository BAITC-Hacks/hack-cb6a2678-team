# Тесты не ходят в сеть: погода только из (пустого) кэша, ML подменяется фикстурами (FixtureMlConfig)
weather:
  offline: true
  cache-dir: build/weather-cache-test
ml:
  base-url: http://localhost:1
  # Фикстуры отложенного теста и SCADA: 01–03.01.2026 (SCADA с 30.12.2025 — для бейзлайна persistence)
  evaluation-dir: src/test/resources/evaluation
  scada-dir: src/test/resources/scada
cycle:
  store-dir: ""
  llm-report: false
  async: false
  retry-delay: 0s
